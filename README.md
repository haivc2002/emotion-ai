cách chạy dự án
run python app.py
vào trình duyệt demo: http://localhost:8000/

api chung (dùng cho cả ảnh upload, base64, url): http://localhost:8000/predict
body 
{
    "image": "base64_image/url/file"
}